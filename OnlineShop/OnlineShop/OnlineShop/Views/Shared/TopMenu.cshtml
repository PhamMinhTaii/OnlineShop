@model List<Models.EF.Menu>
<div class="call">
    <p>Liên hệ <span class="number">01676 738 370</span></p>
</div>
<div class="account_desc">
    <ul>
        @foreach(var item in Model)
        {
            <li><a href="@item.Link" target="@item.Target">@item.Text</a></li>
        }
    </ul>
</div>
<div class="clear"></div>